<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>
<h1>Events</h1>

<div id="events">
  <% @events.each do |event| %>
    <%= render event %>
    <p>
      <%= link_to 'Details', event_path(event) %>
    </p>
  <% end %>
</div>